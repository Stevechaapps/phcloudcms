{
  "name": "phcloudcms",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/generate-theme-index.js && wrangler dev --d1 DB --kv CACHE",
    "build": "node scripts/generate-theme-index.js && tsc --noEmit",
    "lint": "prettier --check src/",
    "lint:fix": "prettier --write src/"
  },
  "dependencies": {
    "hono": "^4.12.29"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260712.1",
    "prettier": "^3.6.2",
    "typescript": "^7.0.2",
    "wrangler": "^4.110.0"
  }
}
